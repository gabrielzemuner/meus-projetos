let valor = 30;
console.log(valor);

// por conta da inferência de tipo na variável (let valor = 30, portanto => number) não conseguimos definir ela novamente em outro tipo (por ex: valor = 'João' => string)
// valor = 'João'
console.log(valor);
